<%= simple_form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :year %><br />
	
    <%= f.input :week %><br />
	
    <%= f.association :away_team, collection: Team.all(order: 'name'), prompt: "Away Team" %><br />
	
    <%= f.association :home_team, collection: Team.all(order: 'name'), prompt: "Home Team" %><br />
	
    <%= f.input :location %><br />
	
    <%= f.input :line %><br />
	
    <%= f.input :home_score %><br />
	
    <%= f.input :away_score %><br />
	
    <%= f.input :date %><br />

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
