<div class="standings-wrapper">
	<h3>Standings</h3>
	<div class="standings-table-wrapper">
		<div class="table-scroll">
			<table>
				<thead>
					<tr>
						<th><span class="text">User</span></th>
						<th><span class="th-record">Week</span></th>
						<th><span class="th-record">Overall</span></th>
                        <th><span class="th-record">Balance</span></th>
                        <th><span class="th-record">Weekly Wagers</span></th>
					</tr>
				</thead>
				<tbody>
					<% @users.map do |user| %>
						<tr>
                            <% if user.name.nil? %>
							    <td class="td-name"><%= user.email %></td>
                            <% else %>
                                <td class="td-name"><%= user.name %></td>
                            <% end %>
							<td class="td-record"><%= user.pickem_picks_record_by_year_and_week(@year, @week) %></td>
							<td class="td-record"><%= user.pickem_picks_record_by_year(@year) %></td>
                            <td class="td-record"><a href="<%= "/user_wagers/#{@year}/#{user.id}" %>"><%= user.find_balance_by_year(@year) %></a></td>
                            <td class="td-record"><a href="<%= "/user_wagers/#{@year}/#{@week}/#{user.id}" %>">
                              <%= user.find_submitted_balance_by_year_and_week(@year, @week) %></a></td>
						</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>
